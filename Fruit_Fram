-- [[ ZUS-HUB 水果框架 | GLOBAL CLIENT PRODUCTION VERSION ]]

-- Ngăn chặn trùng lặp UI khi chạy lại script (Anti Re-execution)
if _G.ZusHubExecuted then
    pcall(function()
        local coreGui = game:GetService("CoreGui")
        if coreGui:FindFirstChild("Orion") then
            coreGui.Orion:Destroy()
        end
    end)
end
_G.ZusHubExecuted = true

local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()

-- Cấu hình hệ thống toàn cục (Global Environment)
getgenv().ZusHub = {
    Version = "Premium VIP v2.6 (Global)",
    Settings = {
        AutoFarmFruit = false,
        FruitESP = false,
        FastScan = true,
        AntiBanActive = true
    }
}

local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Stats = game:GetService("Stats")
local LocalPlayer = Players.LocalPlayer

-- [[ HỆ THỐNG BẢO VỆ TOÀN DIỆN CLIENT ]]
local function InitiateAntiBan()
    if not getgenv().ZusHub.Settings.AntiBanActive then return end
    if hookmetamethod and checkcaller then
        local oldNamecall; oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
            local method = getnamecallmethod()
            if not checkcaller() and (method == "Kick" or method == "kick" or method == "Crash") then
                return nil
            end
            return oldNamecall(self, ...)
        end))
    end
end

-- [[ BỘ QUÉT DỮ LIỆU BUFFER CHỐNG LAG ]]
local DataAnalyzer = {}
function DataAnalyzer:GetServerFruits()
    local detectedFruits = {}
    local children = Workspace:GetChildren()
    for i = 1, #children do
        local obj = children[i]
        if obj:IsA("Tool") and (obj.Name:find("Fruit") or obj.Name:find("Trái")) then
            table.insert(detectedFruits, obj)
        end
    end
    return detectedFruits
end

-- [[ TWEEN CHUYỂN ĐỘNG - TỰ ĐỘNG CẬP NHẬT KHI LUÂN CHUYỂN NHÂN VẬT ]]
local function SmoothTween(targetCFrame, speed)
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local rootPart = character:WaitForChild("HumanoidRootPart", 10)
    if not rootPart then return end
    
    local distance = (rootPart.Position - targetCFrame.Position).Magnitude
    local duration = distance / speed
    
    local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear, Enum.EasingDirection.Out)
    local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
    
    local connection
    connection = RunService.Heartbeat:Connect(function()
        -- Kiểm tra nếu tắt tính năng hoặc nhân vật bị chết/đổi trạng thái thì hủy tween ngay lập tức
        if not getgenv().ZusHub.Settings.AutoFarmFruit or not rootPart.Parent or not character:FindFirstChild("Humanoid") or character.Humanoid.Health <= 0 then
            tween:Cancel()
            if connection then connection:Disconnect() end
        end
    end)
    
    tween:Play()
    tween.Completed:Wait()
    if connection then connection:Disconnect() end
end

-- [[ VÒNG LẶP AUTO FARM TOÀN CLIENT ]]
local function StartFruitFarm()
    task.spawn(function()
        while getgenv().ZusHub.Settings.AutoFarmFruit do
            local character = LocalPlayer.Character
            if character and character:FindFirstChild("Humanoid") and character.Humanoid.Health > 0 then
                local fruits = DataAnalyzer:GetServerFruits()
                if #fruits > 0 then
                    for i = 1, #fruits do
                        local fruit = fruits[i]
                        if not getgenv().ZusHub.Settings.AutoFarmFruit then break end
                        if fruit and fruit.Parent == Workspace and fruit:FindFirstChild("Handle") then
                            SmoothTween(fruit:GetPivot(), 300)
                            task.wait(0.1)
                        end
                    end
                else
                    task.wait(0.5)
                end
            else
                -- Chờ nhân vật hồi sinh hoàn toàn nếu bị chết (Chống gãy script)
                task.wait(1)
            end
            task.wait(0.1)
        end
    end)
end

-- [[ HỆ THỐNG VISUALS / GLOBAL ESP FRUIT ]]
local function ApplyFruitESP(fruit)
    if not fruit:FindFirstChild("ZusESP") then
        local billboard = Instance.new("BillboardGui", fruit)
        billboard.Name = "ZusESP"
        billboard.AlwaysOnTop = true
        billboard.Size = UDim2.new(0, 160, 0, 40)
        
        local textLabel = Instance.new("TextLabel", billboard)
        textLabel.Size = UDim2.new(1, 0, 1, 0)
        textLabel.BackgroundTransparency = 1
        textLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
        textLabel.TextSize = 13
        textLabel.Font = Enum.Font.GothamBold
        textLabel.Text = "🔮 " .. fruit.Name
    end
end

local function StartFruitESP()
    task.spawn(function()
        while getgenv().ZusHub.Settings.FruitESP do
            local fruits = DataAnalyzer:GetServerFruits()
            for i = 1, #fruits do
                local fruit = fruits[i]
                if fruit.Parent == Workspace then
                    ApplyFruitESP(fruit)
                end
            end
            task.wait(1.5)
        end
    end)
end

local function RemoveFruitESP()
    local fruits = DataAnalyzer:GetServerFruits()
    for i = 1, #fruits do
        local fruit = fruits[i]
        if fruit:FindFirstChild("ZusESP") then
            fruit.ZusESP:Destroy()
        end
    end
end

-- [[ KHỞI TẠO MENU PREMIUM VIP (ORION UI) ]]
InitiateAntiBan()

local Window = OrionLib:MakeWindow({
    Name = "Zus-hub | 水果框架 (Premium VIP)", 
    HidePremium = false, 
    SaveConfig = true, 
    ConfigFolder = "ZusHub_Global_BloxFruits",
    IntroText = "Zus-hub Premium Loading..."
})

-- TAB: TỰ ĐỘNG FARM
local FarmTab = Window:MakeTab({
    Name = "Auto Farm Fruit",
    Icon = "rbxassetid://4483345998",
    Premium = false
})

FarmTab:AddSection({ Name = "⚡ Khai Thác Trái Ác Quỷ Siêu Tốc" })

FarmTab:AddToggle({
    Name = "Tự động Farm Fruit (Tween Mượt)",
    Default = false,
    Callback = function(Value)
        getgenv().ZusHub.Settings.AutoFarmFruit = Value
        if Value then
            StartFruitFarm()
        end
    end
})

FarmTab:AddToggle({
    Name = "Quét Siêu Nhanh (Fast Scan)",
    Default = true,
    Callback = function(Value)
        getgenv().ZusHub.Settings.FastScan = Value
    end
})

-- TAB: ĐỊNH VỊ (ESP)
local VisualsTab = Window:MakeTab({
    Name = "Visuals / ESP",
    Icon = "rbxassetid://4483345998",
    Premium = false
})

VisualsTab:AddSection({ Name = "👁️ Định Vị Trái Ác Quỷ Toàn Server" })

VisualsTab:AddToggle({
    Name = "Bật ESP Fruit Siêu Xịn",
    Default = false,
    Callback = function(Value)
        getgenv().ZusHub.Settings.FruitESP = Value
        if Value then
            StartFruitESP()
        else
            RemoveFruitESP()
        end
    end
})

-- TAB: THÔNG TIN SERVER
local DataTab = Window:MakeTab({
    Name = "Server Info",
    Icon = "rbxassetid://4483345998",
    Premium = false
})

DataTab:AddSection({ Name = "📊 Hệ Thống Phân Tích Dữ Liệu Realtime" })

local FruitCountLabel = DataTab:AddLabel("Trái ác quỷ hiện có: Đang tính toán...")
local PingLabel = DataTab:AddLabel("Ping hệ thống: -- ms")

task.spawn(function()
    while true do
        if Window then
            local fruits = DataAnalyzer:GetServerFruits()
            local pV = Stats.Network.ServerStatsItem["Data Ping"]:GetValueString()
            
            FruitCountLabel:Set("Trái ác quỷ trong Server: " .. tostring(#fruits))
            PingLabel:Set("Độ trễ phản hồi (Ping): " .. string.sub(pV, 1, 5) .. " ms")
        end
        task.wait(1.2)
    end
end)

OrionLib:Init()
